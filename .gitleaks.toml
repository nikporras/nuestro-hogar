# gitleaks configuration for Nuestro Hogar.
# The app uses no secrets; this scanner runs purely as repo hygiene so a
# credential can never be committed by accident. Default ruleset, no allowlist.

[extend]
useDefault = true
